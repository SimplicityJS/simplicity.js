const SimplicityClient = require("./classes/SimplicityClient");
const Util = require("./classes/Util.js");

/**
 * SimplicityClient, custom events, command loader, client shard, error handling and utility functions
 * @namespace
 */
module.exports = {
  SimplicityClient,
  Util
};
